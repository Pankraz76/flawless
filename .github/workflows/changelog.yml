name: Changelog 📋
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    name: Print 📝
    steps:
      - name: Checkout 📥
        uses: actions/checkout@v6
      - name: Java ☕️
        uses: actions/setup-java@v5
        with:
          java-version: 21
          distribution: 'temurin'
      - name: Gradle 🐘
        uses: gradle/actions/setup-gradle@v5
      - name: Changelog Print 🖨️
        run: ./gradlew changelogPrint
